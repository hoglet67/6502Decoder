This is a relatively simple USB high speed input or output pipe 
program. 

It either reads data from stdin and sends it over the USB or 
reads data from the USB and sends it to stdout. 
Works with Cypress FX2 chip, hence the name fx2pipe. 

For build instructions, please see INSTALL. 

Wolfgang
